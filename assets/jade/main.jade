include _config.jade
include _mixins.jade 

doctype html
html(lang="es")
  head
    +head(titulo + "" , stylesheets, keywords, description)
  body
    .dbase
    +sliding-menu
    +navigation(menu)
    
    +hrcolors('top')
    +hrcolors('bottom')

    +scripts 
